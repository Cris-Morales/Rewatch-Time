import React, { FC } from 'react';

const ArcsForm = ({}) => {
  return (
    // props
    <div className='flex w-screen overflow-auto h-card'>
      <div>test</div>
    </div>
  );
};

export default ArcsForm;
